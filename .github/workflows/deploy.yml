name: Deploy Goodbook to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:   # allow manual trigger from GitHub UI

permissions:
  contents: read
  pages: write
  id-token: write

# Only one deploy runs at a time; the old version stays live until the new
# one is fully uploaded and atomically swapped in by GitHub Pages.
concurrency:
  group: "pages"
  cancel-in-progress: false   # never cancel an in-progress deploy

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: pip install reportlab

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: 1.9.12

      - name: Render Quarto site
        run: quarto render

      - name: Generate PDFs
        run: |
          python generate_ledger.py
          python generate_spellbook.py
          cp Tiberius-Ledger.pdf docs/
          cp Tiberius-Spellbook.pdf docs/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
